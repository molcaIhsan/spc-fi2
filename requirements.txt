# Runtime dependencies. detector.py/preprocessing.py/models.py/message_parser.py
# are pure Python (zero deps by design) -- these are for the Flink job + config loading.
#
# 1.20.1 confirmed as the real target cluster's version (ajinomoto-etl-flink's
# docker/Dockerfile base image, checked directly) -- was wrongly guessed as
# 1.17.1/1.18.1 earlier before that was confirmed. PyFlink's client version
# must match the cluster's Flink version, or job submission fails.
apache-flink==1.20.1

# For loading spc_line_config at job-submission time (config.py::fetch_line_calibrations)
# -- a plain Python DB connection, separate from the Java JDBC driver the Flink
# JDBC sink itself uses to WRITE spc_readings (see Dockerfile).
psycopg2-binary==2.9.9
